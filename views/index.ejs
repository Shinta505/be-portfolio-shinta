<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio & CMS API Documentation</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="bg-slate-50 text-slate-800 min-h-screen flex flex-col justify-between selection:bg-indigo-600 selection:text-white">

    <!-- Container Utama -->
    <div class="max-w-5xl mx-auto w-full px-4 py-12 grow">

        <!-- Header Judul -->
        <div class="text-center mb-10">
            <h1 class="text-3xl sm:text-4xl font-extrabold text-blue-600 mb-2 tracking-tight">
                Portfolio & CMS Backend API
            </h1>
            <p class="text-slate-500 text-sm sm:text-base">
                Dokumentasi lengkap endpoint manajemen portofolio, autentikasi admin, galeri karya, artikel, riwayat, dan pesan masuk[cite: 1].
            </p>
        </div>

        <!-- Environment Status Card -->
        <div class="bg-white border border-slate-200 rounded-xl p-5 mb-8 shadow-sm">
            <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
                <div>
                    <span class="text-xs font-bold uppercase tracking-wider text-slate-400">Environment Active</span>
                    <div class="flex items-center gap-2 mt-1">
                        <span class="w-3 h-3 bg-emerald-500 rounded-full animate-pulse"></span>
                        <span class="text-sm font-semibold text-slate-700">Node.js & Supabase PostgreSQL Connected</span>
                    </div>
                </div>
                <div class="text-center sm:text-right">
                    <span class="text-xs font-semibold text-slate-500">Base URL API:</span>
                    <code class="block text-sm font-mono text-blue-600 bg-blue-50 px-3 py-1 rounded border border-blue-100 mt-1">/api</code>
                </div>
            </div>
        </div>

        <!-- Daftar Menu Accordion / Endpoint API -->
        <div class="space-y-4">

            <!-- 1. AUTH API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('auth-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Authentication API (Admin CMS)
                    </span>
                    <svg id="auth-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="auth-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/login</code>
                        </div>
                        <span class="text-xs text-slate-500">Autentikasi masuk admin dan pembuatan token JWT[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/me</code>
                        </div>
                        <span class="text-xs text-slate-500">Memuat sesi data profil admin aktif (Protected)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/logout</code>
                        </div>
                        <span class="text-xs text-slate-500">Mengakhiri sesi login admin[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 2. PROFILE API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('profile-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Profile API (Tentang Saya)
                    </span>
                    <svg id="profile-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="profile-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/profile</code>
                        </div>
                        <span class="text-xs text-slate-500">Mengambil data biodata profil utama (Publik)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-amber-500 text-white text-xs font-bold rounded">PUT</span>
                            <code class="text-sm font-mono text-slate-700">/api/profile</code>
                        </div>
                        <span class="text-xs text-slate-500">Memperbarui atau membuat profil (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 3. PROJECT API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('project-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Project API (Galeri Karya)
                    </span>
                    <svg id="project-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="project-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/projects</code>
                        </div>
                        <span class="text-xs text-slate-500">Mengambil daftar seluruh projek / filter kategori[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/projects/:identifier</code>
                        </div>
                        <span class="text-xs text-slate-500">Detail projek berdasarkan UUID atau Slug[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/projects</code>
                        </div>
                        <span class="text-xs text-slate-500">Tambah projek baru dengan upload gambar (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PUT</span>
                            <code class="text-sm font-mono text-slate-700">/api/projects/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Perbarui data projek (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/projects/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Hapus projek beserta file terkait (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 4. ARTICLE API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('article-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Article API (Blog & Artikel)
                    </span>
                    <svg id="article-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="article-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/articles</code>
                        </div>
                        <span class="text-xs text-slate-500">Mengambil seluruh daftar artikel / filter status[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/articles/:identifier</code>
                        </div>
                        <span class="text-xs text-slate-500">Detail artikel berdasarkan UUID atau Slug[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/articles</code>
                        </div>
                        <span class="text-xs text-slate-500">Membuat artikel baru (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PUT</span>
                            <code class="text-sm font-mono text-slate-700">/api/articles/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Memperbarui artikel (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/articles/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Menghapus artikel (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 5. CONTACT API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('contact-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Contact API (Formulir Kontak)
                    </span>
                    <svg id="contact-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="contact-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/contact</code>
                        </div>
                        <span class="text-xs text-slate-500">Kirim pesan baru (Public dengan Rate Limiter)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/contacts</code>
                        </div>
                        <span class="text-xs text-slate-500">Mendapatkan seluruh daftar pesan masuk (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/contacts/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Detail pesan berdasarkan UUID (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/contacts/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Menghapus pesan berdasarkan UUID (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 6. RESUME API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('resume-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Resume / CV API
                    </span>
                    <svg id="resume-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="resume-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/resumes</code>
                        </div>
                        <span class="text-xs text-slate-500">Ambil daftar semua resume / CV[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/resumes/active</code>
                        </div>
                        <span class="text-xs text-slate-500">Ambil resume aktif utama untuk publik[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/resumes</code>
                        </div>
                        <span class="text-xs text-slate-500">Tambah data resume baru (Protected)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PUT</span>
                            <code class="text-sm font-mono text-slate-700">/api/resumes/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Perbarui data resume (Protected)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PATCH</span>
                            <code class="text-sm font-mono text-slate-700">/api/resumes/:uuid/active</code>
                        </div>
                        <span class="text-xs text-slate-500">Atur resume sebagai aktif utama (Protected)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/resumes/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Hapus data resume (Protected)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 7. EXPERIENCE API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('experience-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Experience API (Riwayat Pekerjaan)
                    </span>
                    <svg id="experience-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="experience-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/experiences</code>
                        </div>
                        <span class="text-xs text-slate-500">Ambil seluruh data riwayat pekerjaan (Publik)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/experiences/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Detail riwayat pekerjaan berdasarkan UUID[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/experiences</code>
                        </div>
                        <span class="text-xs text-slate-500">Tambah riwayat pekerjaan baru (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PATCH</span>
                            <code class="text-sm font-mono text-slate-700">/api/experiences/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Perbarui riwayat pekerjaan (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/experiences/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Hapus riwayat pekerjaan (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 8. EDUCATION API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('education-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Education API (Riwayat Pendidikan)
                    </span>
                    <svg id="education-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="education-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/educations</code>
                        </div>
                        <span class="text-xs text-slate-500">Ambil seluruh data riwayat pendidikan (Publik)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/educations/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Detail riwayat pendidikan berdasarkan ID[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/educations</code>
                        </div>
                        <span class="text-xs text-slate-500">Tambah riwayat pendidikan baru (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PATCH</span>
                            <code class="text-sm font-mono text-slate-700">/api/educations/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Perbarui riwayat pendidikan (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/educations/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Hapus riwayat pendidikan (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 9. CERTIFICATION API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('certification-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Certification API (Sertifikasi & Lisensi)
                    </span>
                    <svg id="certification-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="certification-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/certifications</code>
                        </div>
                        <span class="text-xs text-slate-500">Ambil seluruh data sertifikasi (Publik)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/certifications/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Detail sertifikasi berdasarkan UUID[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/certifications</code>
                        </div>
                        <span class="text-xs text-slate-500">Tambah sertifikasi baru (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PATCH</span>
                            <code class="text-sm font-mono text-slate-700">/api/certifications/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Perbarui sertifikasi (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/certifications/:id</code>
                        </div>
                        <span class="text-xs text-slate-500">Hapus sertifikasi (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

            <!-- 10. SKILL API -->
            <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden transition duration-200">
                <button onclick="toggleAccordion('skill-content')" class="w-full px-6 py-4 flex justify-between items-center text-left font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none">
                    <span class="flex items-center gap-2">
                        <span class="w-2 h-2 bg-blue-600 rounded-full"></span>
                        Skill API (Keahlian & Teknologi)
                    </span>
                    <svg id="skill-icon" class="w-5 h-5 text-slate-400 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </button>
                <div id="skill-content" class="hidden px-6 pb-6 pt-2 border-t border-slate-100 space-y-3">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/skills</code>
                        </div>
                        <span class="text-xs text-slate-500">Ambil seluruh data keahlian dan teknologi (Publik)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
                            <code class="text-sm font-mono text-slate-700">/api/skills/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Detail keahlian berdasarkan UUID[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-green-600 text-white text-xs font-bold rounded">POST</span>
                            <code class="text-sm font-mono text-slate-700">/api/skills</code>
                        </div>
                        <span class="text-xs text-slate-500">Tambah keahlian baru (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-indigo-600 text-white text-xs font-bold rounded">PATCH</span>
                            <code class="text-sm font-mono text-slate-700">/api/skills/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Perbarui keahlian (Protected - Admin)[cite: 1]</span>
                    </div>
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between p-3 bg-slate-50 rounded-lg border border-slate-100 gap-2">
                        <div class="flex items-center gap-3">
                            <span class="px-2.5 py-1 bg-rose-600 text-white text-xs font-bold rounded">DELETE</span>
                            <code class="text-sm font-mono text-slate-700">/api/skills/:uuid</code>
                        </div>
                        <span class="text-xs text-slate-500">Hapus keahlian (Protected - Admin)[cite: 1]</span>
                    </div>
                </div>
            </div>

        </div>

    </div>

    <!-- Footer -->
    <footer class="max-w-5xl mx-auto w-full px-4 py-6 border-t border-slate-200 text-center text-xs text-slate-400">
        <p>&copy; 2026 Portfolio Backend System. Running on Node.js, Express, Sequelize & Supabase.</p>
    </footer>

    <!-- Script Accordion Interactive -->
    <script>
        function toggleAccordion(id) {
            const content = document.getElementById(id);
            const icon = document.getElementById(id.replace('-content', '-icon'));

            if (content.classList.contains('hidden')) {
                content.classList.remove('hidden');
                icon.classList.add('rotate-180');
            } else {
                content.classList.add('hidden');
                icon.classList.remove('rotate-180');
            }
        }
    </script>
</body>

</html>